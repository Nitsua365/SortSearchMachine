
#include <cmath>

struct SearchAlgorithms {
    
    int BinarySearch(int a[], int key, const int& SIZE);
    int LinearSearch(int a[], int key, const int& SIZE);
    
    bool verifySort(int a[], const int& SIZE);
    
};
